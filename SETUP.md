# 🚀 Налаштування для Локальної Розробки

Цей файл пояснює, що було автоматично налаштовано в проекті для зручної роботи.

## ✨ Що працює автоматично

### 1. Автоформатування коду (Prettier)

**Коли працює:** При збереженні файлу в VS Code (Ctrl+S / Cmd+S)

**Що робить:** Автоматично вирівнює код, додає/прибирає пробіли, вирівнює лапки

**Налаштування:** Див. `.vscode/settings.json`

**Примітка:** Для роботи потрібне розширення [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode) для VS Code

---

### 2. Перевірка перед коммітом (Husky)

**Коли працює:** Автоматично при кожному `git commit`

**Що перевіряє:**
- 📝 Чи код відповідає стандартам (ESLint)
- 🧪 Чи проходять тести (Vitest)
- 🔒 Чи немає критичних вразливостей (npm audit)

**Що робити, якщо коммміт заблоковано:**
```bash
# Дивись що саме не так у виведенні терміналу
# Найчастіше це:

# 1. Помилка лінтингу → виправ автоматично:
npm run lint:fix

# 2. Тести падають → перевір що зламалось:
npm run test

# 3. Вразливості → оновити пакети:
npm audit fix
```

---

### 3. GitHub Actions CI

**Коли працює:** При push в GitHub (на будь-який бранч `claude/**`, `main`, `master`)

**Що перевіряє:**
1. Security audit
2. Lint code
3. Format check
4. Tests
5. Build
6. SEO verification
7. Bundle size check

**Де дивитись результати:** GitHub → вкладка "Actions"

**Що робити, якщо червоний хрестик:**
1. Клікни на червоний хрестик в GitHub
2. Подивись який крок упав
3. Запусти цей крок локально (div. команди нижче)

---

## 📝 Корисні команди

### Щоденна робота
```bash
# Старт dev сервера (як завжди)
npm run start

# Перевірити код перед комітом (вручну)
npm run lint        # Тільки перевірка
npm run lint:fix    # Виправити автоматично

# Перевірити форматування
npm run format:check    # Тільки перевірка
npm run format          # Виправити автоматично

# Запустити тести
npm run test
```

### Перед деплоєм
```bash
# Повна перевірка (те саме що робить CI)
npm run verify
# Це запустить: audit + tests + build + SEO check
```

### Якщо щось зламалось
```bash
# Виправити код
npm run lint:fix && npm run format

# Видалити node_modules і переставити все
rm -rf node_modules package-lock.json
npm install

# Очистити кеші
rm -rf dist .astro src/lib/.cache
npm run cache:build
```

---

## 🔧 Налаштування VS Code

### Обов'язкові розширення
1. **Prettier** - Форматування коду
   - ID: `esbenp.prettier-vscode`
   - Встановити: Ctrl+P → `ext install esbenp.prettier-vscode`

2. **ESLint** - Перевірка коду
   - ID: `dbaeumer.vscode-eslint`
   - Встановити: Ctrl+P → `ext install dbaeumer.vscode-eslint`

3. **Astro** - Підсвітка синтаксису
   - ID: `astro-build.astro-vscode`
   - Встановити: Ctrl+P → `ext install astro-build.astro-vscode`

### Корисні налаштування
Файл `.vscode/settings.json` вже створений з оптимальними налаштуваннями:
- Автоформатування при збереженні
- Автовиправлення ESLint
- Правильні форматери для кожного типу файлів

---

## 🎯 Типовий робочий процес

### Звичайний день:
```bash
1. git pull                  # Отримати останні зміни
2. npm run start            # Запустити dev сервер
3. [Пишеш контент/код]
4. Ctrl+S                   # Зберегти (автоформатування!)
5. git add .
6. git commit -m "..."      # Автоматичні перевірки спрацюють
7. git push                 # GitHub Actions перевірить ще раз
```

### Якщо щось не так:
```bash
# Швидке виправлення перед комітом
npm run lint:fix && npm run format && npm run test

# Або повна перевірка
npm run verify
```

---

## ❓ FAQ

### Чому коммміт так довго виконується?
Це husky перевіряє код, тести та безпеку. Зазвичай 10-30 секунд.

### Можна пропустити перевірки?
Так, але **не рекомендується**:
```bash
git commit --no-verify -m "message"
```

### Як відключити автоформатування?
В `.vscode/settings.json` поставити:
```json
"editor.formatOnSave": false
```

### Що робити якщо GitHub Actions червоний?
1. Клікни на червоний хрестик
2. Подивись яка перевірка упала
3. Запусти її локально і виправ
4. Зроби новий коммміт

---

## 🆘 Проблеми і рішення

### "npm run lint" падає
```bash
# Спробуй автовиправлення
npm run lint:fix

# Якщо не допомогло - подивись помилки і виправ вручну
```

### "npm run test" падає
```bash
# Запусти в watch mode щоб бачити деталі
npm run test:watch

# Або подивись конкретний тест
npm run test -- tests/seo.test.ts
```

### Git hooks не працюють
```bash
# Переініціалізуй husky
rm -rf .husky
npm run prepare
```

---

## 📚 Додаткова інформація

- **ESLint конфіг:** `eslint.config.js`
- **Prettier конфіг:** `.prettierrc`
- **Husky hooks:** `.husky/pre-commit`
- **GitHub Actions:** `.github/workflows/ci.yml`
- **VS Code налаштування:** `.vscode/settings.json`

---

**Створено:** Claude AI для максимально простої підтримки проекту
**Мета:** Ти пишеш контент, а всі перевірки відбуваються автоматично
